# Copy this file to .env (for all modes) or .env.production/.env.development
# Vite only exposes variables prefixed with VITE_

# Backend base URL WITHOUT trailing slash (the client will append /api)
VITE_API_URL=https://wishes-rhvh.onrender.com
