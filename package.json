{
  "name": "wishes-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run -ws dev",
    "build": "npm run -ws build",
    "lint": "npm run -ws lint",
    "start": "npm --workspace backend run start && npm --workspace frontend run preview"
  }
}
